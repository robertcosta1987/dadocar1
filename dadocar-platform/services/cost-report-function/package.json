{
  "name": "dadocar-cost-report-function",
  "version": "0.1.0",
  "private": true,
  "description": "Azure Function App: monthly Azure cost report for rg-dadocar-dev-brs. Timer (1st of month) + HTTP trigger. Queries Cost Management, builds a per-resource report and a meter-level report, publishes a static HTML page (indexed by month), saves a .txt to blob, and emails stakeholders via ACS.",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"no unit tests yet\" && exit 0"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@azure/functions": "^4.5.1",
    "@azure/identity": "^4.4.1",
    "@azure/storage-blob": "^12.24.0",
    "@azure/communication-email": "^1.0.0"
  }
}
