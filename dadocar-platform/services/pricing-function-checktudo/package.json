{
  "name": "dadocar-pricing-function-checktudo",
  "version": "0.1.0",
  "private": true,
  "description": "Azure Function App: CheckTudo vehicle-data integration. Looks up a Brazilian plate (or chassi) against the CheckTudo async query API for a selectable product (querycode) and returns the result JSON. Runs independently from the KBB/Molicar pricing function.",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"no unit tests yet\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@azure/functions": "^4.5.1",
    "@azure/identity": "^4.4.1",
    "@azure/keyvault-secrets": "^4.8.0"
  }
}
